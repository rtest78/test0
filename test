<xsl:when test="name() = 'SentiLinkResponse'">
    <xsl:text>{</xsl:text>
    <xsl:call-template name="process-children">
        <xsl:with-param name="nodes"
            select="*[* or @* or normalize-space(translate(., '&quot;', ''))]"/>
    </xsl:call-template>
    <xsl:text>}</xsl:text>
</xsl:when>
